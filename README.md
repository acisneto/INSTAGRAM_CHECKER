The checked results are supposed to show up in a toast windows notification. 

INSTALL REQUIREMENTS BEFORE RUNNING!

-- python -m pip install win10toast
-- python -m pip install requests

Also tested succefully with PyInstaller. 

